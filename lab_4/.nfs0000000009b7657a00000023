#!/bin/bash
SIZE=(40000000000)
mpicc -o pi_par pi_par.c

for s in ${SIZE[@]}; do
	for ((i=1; i<=8; i++)); do
        	declare -i n=$(( s * i))
		mpiexec -machinefile ./allnodes -np $i ./pi_par $n >>output.txt	 
	done
done

